[package]
name = "mdtodo"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
chrono = "0.4"
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
regex = "1.10"
dirs = "5.0"

[dev-dependencies]
tempfile = "3.15"
